[project]
name = "qtic-backend"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "openai",
    "supabase",
    "apscheduler",
    "resend",
    "python-dotenv",
    "pydantic",
]